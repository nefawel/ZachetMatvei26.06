# Challenge 08

Метод `Fizz_Buzz.fizzbuzz(self, max_num)` отвечает за вывод результата FizzBuzz внутри объекта класса.

Ошибка: метод класса `fizzbuzz` был объявлен без параметра `self`, поэтому при вызове у объекта возникал `TypeError`.
Решение: добавил `self` в сигнатуру метода.
