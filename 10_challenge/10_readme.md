# Challenge 10

Функция `fizzbuzz(max_num)` в модуле `fizzbuzz.py` отвечает за основной вывод чисел по правилам FizzBuzz.

Ошибка: после `import fizzbuzz` код пытался вызвать модуль как функцию.
Решение: изменил вызов на `fizzbuzz.fizzbuzz(100)` и поправил вывод `buzz` в модуле.
