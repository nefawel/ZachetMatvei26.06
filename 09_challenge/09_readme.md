# Challenge 09

Функция `fizzbuzz(max_num)` отвечает за последовательный проход по диапазону чисел и полный вывод всех подходящих строк FizzBuzz.

Ошибка: цикл начинался с `6`, из-за чего программа пропускала первые корректные значения `3 fizz` и `5 buzz`.
Решение: вернул начало диапазона к `1`.
