# Challenge 12

Метод `FizzBuzz.fizzbuzz(self, max_num)` отвечает за вывод FizzBuzz, используя параметры класса, заданные в `__init__`.

Ошибка: в инициализаторе было задано `self.num2 = 4`, поэтому `buzz` печатался для кратных 4, а не 5.
Решение: исправил значение на `5`.
