# Challenge 02

Ошибка: функция `fizzbuzz` вызывалась без обязательного аргумента `max_num`.
Решение: передал корректное значение `100` и заодно привёл вывод `buzz` к ожидаемому виду в нижнем регистре.
